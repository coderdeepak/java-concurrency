In this example a thread PrimeNumberGenerator keeps on testing prime number until it is interrupted.

isInterrupted() method which is defined in Thread class is used to check if a thread is interrupted or not.

 